quay.io/swade1987/jenkins
==================

Docker image for Jenkins (includes the Apprenda plugin)

To pull this image:
`docker pull quay.io/swade1987/jenkins`

Example usage:
`docker run -p 8080 -d quay.io/swade1987/jenkins`